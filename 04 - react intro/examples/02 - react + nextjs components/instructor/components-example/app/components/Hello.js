export default function Hello() {
    // This is a 'static component', meaning it returns the same thing every time
    // regardless of any data/props that something else might try to pass into it.
    return (
        <h3>Hey, here's our first component.</h3>
    )
}
