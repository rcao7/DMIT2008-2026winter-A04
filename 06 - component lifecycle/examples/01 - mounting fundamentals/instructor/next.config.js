/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Don't just disable strict mode; it's useful!
     This is just to prove/demo that it makes components render, and effects fire, twice.
  */
  reactStrictMode: false,
}

module.exports = nextConfig
